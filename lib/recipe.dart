class Recipe {
  String title;
  String user;
  String image;
  String description;
  bool isFavorite;
  int favoriteCount;

  Recipe(this.title, this.user, this.image, this.description,
      this.isFavorite, this.favoriteCount);
}